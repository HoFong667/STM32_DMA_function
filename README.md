# STM32_DMA_function

Target board: NUCLEO-F446ZE

1. I2C_TX DMA mode
2. I2C_RX DMA mode
3. UART DMA mode
4. ADC DMA mode

All drive with Timer (except ADC) + State machine, with low priority setting
